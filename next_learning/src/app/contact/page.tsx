import React from "react"
const contact = () => {
  return (
    <div className=" capitalize text-2xl font-semibold flex h-lvh w-full justify-center items-center">
      <p>i am about contact page</p>
    </div>
  );
};

export default contact;
/* 
* The folder name inside the app/ directory determines the URL path.
* contact/ maps to /contact.
* page.tsx is Mandatory:Every folder representing a route must contain a file named page.tsx or page.jsx.

*/